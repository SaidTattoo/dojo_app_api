name: Ejecución Manual de LS

on:
  workflow_dispatch:

jobs:
  run-ls:
    runs-on: test-runner-api
    steps:
    - name: Clonar repositorio
      uses: actions/checkout@v2

    - name: Ejecutar comando LS
      run: ls -l